version: '3.8'

services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
    command: streamlit run deployment/dashboard.py --server.port 8501 --server.address 0.0.0.0
